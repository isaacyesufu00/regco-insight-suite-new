## Change

In `src/pages/Index.tsx`, hero section (around line 431), update the eyebrow label only:

- `Benchmark · v1.0` → `ledger · v1.0`

No other changes. All styling, layout, spacing, and uppercase transform on the eyebrow remain identical (the existing `textTransform: "uppercase"` will render it as `LEDGER · V1.0`).
